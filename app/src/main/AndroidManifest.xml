<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="wieniacy.w.kaloszach.poznaj2">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="your.package.name.permission.MAPS_RECEIVE" />


    <application
        android:name=".MyAplicationGlobal"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<meta-data-->
            <!--android:name="com.google.android.gms.version"-->
            <!--android:value="AIzaSyAbzTyrGDlZzoteFqmtFctnun8qOet7ygc" />-->
        <!--
                     The API key for Google Maps-based APIs is defined as a string resource.
                     (See the file "res/values/google_maps_api.xml").
                     Note that the API key is linked to the encryption key used to sign the APK.
                     You need a different API key for each encryption key, including the release key that is used to
                     sign the APK for publishing.
                     You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAbzTyrGDlZzoteFqmtFctnun8qOet7ygc" />

        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EventsActivity"
            android:label="Wydarzenia" />
        <activity
            android:name=".FriendsActivity"
            android:label="Znajomi" />
        <activity
            android:name=".GalleryActivity"
            android:label="Galeria" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AddEventActivity" />
        <activity android:name=".DetailsFriendActivity" />
        <activity android:name=".DetailsEventActivity"></activity>

        <activity android:name=".BaseActivity"
            android:label="Podstawa"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity
            android:name=".PeopleAroundActivity"
            android:label="Ludzie w okolicy" />
        <activity
            android:name=".SettingsActivity"
            android:label="Ustawienia"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>
